Contains three "domains"

VandM which is a single shot fhir restful interface

Initial work on FGM which is fhir messaging not restful despite the name of the parent folder

also some ToC material used for testing async routing actor responses required by Chris Berry
This was ITK3/fhir but eventually used mesh as the transport
